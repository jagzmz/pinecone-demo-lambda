{
  "name": "pinecone-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && mkdir -p nodejs && cp -r node_modules nodejs/ && zip -r  clinch_pinecone_usage_demo.zip nodejs && rm -rf nodejs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pinecone-database/pinecone": "^2.2.2",
    "aws-sdk": "^2.1639.0",
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "form-data": "^4.0.0"
  }
}
